/**
 * NOTE:
 * This file is intentionally kept as a harmless module so it does not break
 * TypeScript compilation. Put executable examples in real component/hook files.
 */

export const LOGIN_IMPLEMENTATION_REFERENCE = {
  endpoint: "/v1/auth/login/sanctum",
  tokenField: "access_token",
  redirectAfterLogin: "/dashboard",
};

